# Introduction à Git & GitHub

Ce repository sert de support pour le cours GPWR d’introduction à **Git** et **GitHub**.  
Il est utilisé pour apprendre à travailler seul et à plusieurs sur des fichiers.

---

## Objectifs du cours

À la fin du cours, vous serez capables de :

- Comprendre ce qu’est le **versioning**
- Utiliser les **commandes de base de Git**
- Travailler avec **GitHub**
- Collaborer avec des **branches** et des **Pull Requests**

---

## Git n’est pas un backup

Git est un **outil de gestion de versions**, pas un système de sauvegarde automatique.

- Un commit = une **étape volontaire**
- Git garde l’**historique des changements**
- Git ne sauvegarde rien sans votre action

---

## Workflow utilisé dans ce cours

Cycle de travail simple :

git pull → Modifier → git add → git commit → git push


Commandes utilisées :

- `git clone` : récupérer le projet pour la première fois
- `git status` : savoir où on en est
- `git add` : préparer les changements
- `git commit` : créer une version
- `git push` : partager sur GitHub
- `git pull` : récupérer les changements

---

## Branches & Pull Requests

Pour éviter de casser le projet :

- Une **branche** sert à travailler sans risque
- Une **Pull Request** sert à proposer un changement
- Le **merge** sert à intégrer le travail

👉 On ne travaille pas directement sur `main`.

---

## GitHub Issues

Les **Issues** servent à :

- Décrire un problème
- Proposer une idée
- Discuter avant de modifier le code

---

## GitHub Pages

Ce repository peut être publié avec **GitHub Pages**.

- Le contenu du repo devient un site web
- Un commit peut mettre à jour le site
- Aucun serveur à gérer

---

## Bonnes pratiques

- Committez souvent, pour une seule idée
- Écrivez des messages clairs
- Faites un `git pull` avant de travailler
- Utilisez des branches
- Lisez les messages d’erreur

---

## Objectif pédagogique

Ce repository est volontairement simple.  
Il sert à **pratiquer Git**, pas à produire un projet final.


